# jump_sinatra

```
(downloading and instally rvm/ruby)
git clone https://github.com/dreamingfurther/jump_sinatra.git
cd jump_sinatra
\curl -sSL https://get.rvm.io | bash -s stable
source /Users/dtengdin/.rvm/scripts/rvm

(installing ruby, gems, and npm packages)
rvm install 2.5.1
gem install sinatra
gem install rerun
npm install
npm start

(separate terminal line)
rerun 'ruby server.rb'
```
